var nombre1;
nombre1 = `Juan`
nombre1 = `Pedro`

var edad = 30

console.log(nombre1);
console.log(edad);

var ciudad = "Buenos Aires";
var pais = 'Argentina';
var continente = `América del Sur`;

var dni = `123456789`;

var soltero = true;
var tieneMascota = false;

var marcaAuto = `Toyota`, modeloAuto = `Corolla`, aniAuto = 2020;
var fruta, color, sabor;

console.log(`${nombre1} tiene ${edad} años, vive en ${ciudad} una ciudad de ${pais} que se encuentra en el continente de ${continente}.`)
